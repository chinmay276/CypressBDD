{
  "name": "bdd",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "cypress run --reporter mochawesome",
    "test1": "npx cypress run --env TAGS=@smoke ",
    "test2": "npx cypress run --env allure=true,'TAGS=(@smoke)'",
    "allure": "allure generate allure-results --clean -o allure-reports"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.34.0",
    "cypress": "^12.1.0",
    "cypress-mochawesome-reporter": "^3.7.0"
  },
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^19.1.0",
    "@cypress/browserify-preprocessor": "^3.0.2",
    "allure-commandline": "^2.24.1"
  },
  "cypress-cucumber-preprocessor": {
    "nonGlobalStepDefinitions": true,
    "stepDefinitions": "cypress/e2e/Features/**/*.js"
  }
}
